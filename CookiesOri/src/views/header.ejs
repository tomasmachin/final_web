<!--/views/header.ejs-->
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width , initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="icon" href="/images/favicon.ico">
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <script src="/socket.io/socket.io.js"></script> <!-- Socket.IO -->
    <script>
        document.getElementById('inputField').addEventListener('keydown', function (event) {
            if (event.key === 'Enter') {
                document.getElementById('send-button').click();
            }
        });
    </script>
</head>

<body>
    <div class="wrapper">
        <!-- <div class="main-content"> -->
            <div class="top-content">
                <div class="header">
                    <h1>Web Development studying</h1>
                </div>
                <% if (!user) { %>
                    <div class="nav-bar">
                        <a href="/index" class="btn-nav-bar">Home</a>
                        <a href="/chat" class="btn-nav-bar">Chat</a>
                        <a href="/login" class="btn-nav-bar">Log In</a>
                        <a href="/register" class="btn-nav-bar">Sign Up</a>
                    </div>
                    <% } else { %>
                        <div class="nav-bar">
                            <a href="/index" class="btn-nav-bar">Home</a>
                            <a href="/chat" class="btn-nav-bar">Chat</a>
                            <a href="/logout" class="btn-nav-bar">Log out</a>
                        </div>
                        <% } %>
            </div>
            <div class="main-content">